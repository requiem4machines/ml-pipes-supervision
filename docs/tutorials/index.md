# Learn

This section ports Supervision guides into `ml-pipes` pipelines while keeping
Supervision as the source of detection, annotation, and zone behavior.

Each future guide will link to its upstream Supervision source and show the
equivalent `ml-pipes` pipeline. The current package surface is listed in the
[Reference](../INDEX.md).
